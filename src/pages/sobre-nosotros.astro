---
import Layout from "../layouts/Layout.astro";
import aboutHero from "@images/about/nosotros_hero.png";
import circuitosFondo from "@images/about/circuitos.webp";
import fondoMyV from "@images/about/myv_fondo.webp";
import iconMision from "@icons/icon_mision.webp";
import iconVision from "@icons/icon_vision.webp";
import valoresArray from "@data/valores.data";
import Card from "../components/about/Card.astro";
import bg_card4 from "@images/about/nosotrostami.webp";
---

<Layout
  title="¿Quiénes Somos? | Tami Maquinarias para Emprendedores"
  description="Somos Tami, empresa peruana que apoya a emprendedores con maquinaria y suministros para negocios. Comprometidos con la calidad."
  canonicalUrl="https://tamimaquinarias.com/about/"
>
  {/* 🔹 Banner principal */}
  <section
    class="relative bg-cover bg-center h-screen pt-16 lg:pt-20 flex flex-col justify-center items-start text-white"
    style={`background-image: linear-gradient(to bottom, rgba(0, 120, 111, .75), rgba(0,0,0,.75)), url(${aboutHero.src})`}
  >
    <div
      class="absolute inset-0 z-20 flex flex-col items-center justify-center text-center text-white px-6 bg-gradient-to-b from-[#046A63]/40 to-[#023B37]/60"
    >
      <h2
        class="text-6xl sm:text-5xl lg:text-7xl font-extrabold px-4 sm:px-12 md:pl-16 lg:pl-28 sm:mb-4 max-w-5xl mb-8 leading-relaxed drop-shadow-lg"
      >
        SOBRE NOSOTROS
      </h2>

      <p
        class="text-base sm:text-lg md:text-xl font-medium max-w-5xl drop-shadow-md leading-relaxed"
      >
        Buscamos ayudar en tu negocio, mediante la comercialización de productos
        para emprendimiento, trabajo y decoración, cumpliendo con los estándares
        de calidad.
      </p>
    </div>
  </section>
  <style>
    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(40px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .animate-fadeInUp {
      animation: fadeInUp 1.2s cubic-bezier(0.23, 1, 0.32, 1);
    }
  </style>

  {/* 🔹 Sección Identidad y CTA */}
  <section class="w-full px-0">
    <div
      class="relative overflow-hidden h-[800px] lg:h-[1000px] flex items-center justify-start"
    >
      <div
        class="absolute inset-0 bg-center bg-no-repeat bg-cover scale-100"
        style={`background-image: url('${bg_card4.src}');`}
      >
      </div>
      <div class="absolute inset-0 bg-black/45 z-0"></div>
      <div
        class="relative z-10 text-white text-left max-w-5xl px-4 md:px-8 md:ml-8 lg:ml-16 py-8"
      >
        <h2
          class="text-3xl md:text-5xl lg:text-6xl font-bold mb-8 leading-tight drop-shadow-md"
        >
          ¿POR QUÉ ELEGIR TAMI?
        </h2>

        <p
          class="text-sm md:text-base lg:text-lg font-light mb-10 opacity-95 leading-relaxed drop-shadow-sm text-justify"
        >
          En <span class="font-bold">Tami</span>, nos especializamos en
          soluciones de maquinaria y tecnología para empresas y emprendedores
          que buscan eficiencia, innovación y confianza. Nuestra misión es
          acompañar a nuestros clientes en el crecimiento de sus proyectos,
          brindando productos de calidad y un servicio personalizado.<br /><br
          />

          Nos diferenciamos por nuestro compromiso con la mejora continua, la
          atención cercana y la transparencia en cada proceso.
        </p>
        <div class="flex justify-center md:justify-center">
          <a
            href="/productos"
            title="Ver productos"
            style="background-color: #0C998A;"
            class="hover:bg-teal-700 text-white font-bold text-lg px-8 py-3 rounded-sm transition-all duration-300 inline-flex items-center justify-center shadow-lg cursor-pointer"
          >
            CONTÁCTANOS
          </a>
        </div>
      </div>
    </div>
  </section>

  {/* 🔹 Franja con mensaje “Sobre nuestros propósitos y metas” */}
  <div
    class="bg-cover bg-center h-auto py-16 flex flex-col justify-center items-center"
  >
    <div
      class="bg-teal-700 text-white text-lg sm:text-xl md:text-2xl 2xl:text-5xl py-3 sm:py-5 2xl:py-10 px-8 sm:px-14 2xl:px-20 rounded-full shadow-2xl text-center"
    >
      Sobre nuestros
      <br />
      <span class="font-bold">propósitos y metas</span>
    </div>
  </div>

  {/* 🔹 Sección de Misión y Visión */}
  <section
    class="text-white lg:bg-fixed bg-cover bg-center grid grid-rows-2 grid-cols-2 lg:grid-rows-1 items-center place-items-center h-[70dvh] lg:h-[60dvh] 2xl:h-[50dvh]"
    style={{
      backgroundImage: `linear-gradient(rgba(0,0,0,0.75), rgba(0,0,0,0.75)), url(${fondoMyV.src})`,
    }}
  >
    {/* Misión */}
    <div class="w-full h-full text-center content-center px-1">
      <h2
        class="text-base sm:text-xl lg:text-4xl 2xl:text-8xl font-bold mb-2 2xl:mb-8"
      >
        Misión
      </h2>
      <p class="text-base sm:text-lg lg:text-xl 2xl:text-4xl">
        Entregar soluciones a<br />emprendedores peruanos<br />que compartan
        nuestro<br />deseo de crecer.
      </p>
    </div>
    <img
      src={iconMision.src}
      alt="Icono de Misión"
      class="lg:hidden w-1/2 sm:w-1/4"
      title="Icono de Misión"
      loading="lazy"
    />

    {/* Visión */}
    <img
      src={iconVision.src}
      alt="Icono de Visión"
      class="lg:hidden w-2/3 sm:w-1/3"
      title="Icono de Visión"
      loading="lazy"
    />
    <div class="w-full h-full text-center content-center px-1">
      <h2
        class="text-base sm:text-xl lg:text-4xl 2xl:text-8xl font-bold mb-2 2xl:mb-8"
      >
        Visión
      </h2>
      <p class="text-base sm:text-lg lg:text-xl 2xl:text-4xl">
        Aspiramos a ser la<br />solución confiable más<br />valorada por
        nuestros<br />clientes.
      </p>
    </div>
  </section>

  {/* 🔹 Sección de Valores */}
  <section class="bg-white py-6 lg:py-12 lg:px-6">
    <h2
      class="text-center text-teal-600 text-2xl sm:text-4xl md:text-5xl font-extrabold mb-16"
    >
      VALORES
    </h2>

    {/* Grid de valores dinámicos */}
    <div
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-10 xl:gap-12 justify-items-center"
    >
      {
        valoresArray.map((valor: any) => (
          <div class="flex flex-col items-center w-full">
            <Card
              image={valor.image}
              title={valor.title}
              imageTitle={valor.imageTitle}
              paragraph={valor.paragraph}
            />

            {/* Texto adicional solo en móviles */}
            <div class="block md:hidden w-full px-4 mt-3">
              <p class="text-sm text-teal-700 font-medium text-center leading-relaxed">
                {valor.paragraph}
              </p>
              <hr class="border-t border-teal-800 my-3 w-3/4 mx-auto" />
            </div>
          </div>
        ))
      }
    </div>

    {/* Carrusel para móvil */}
    <div class="md:hidden valores-carousel-container relative">
      <div class="swiper valoresSwiper">
        <div class="swiper-wrapper">
          {
            valoresArray.map((valor: any) => (
              <div class="swiper-slide">
                <div class="flex flex-col items-center w-full px-4">
                  <Card
                    image={valor.image}
                    title={valor.title}
                    imageTitle={valor.imageTitle}
                    paragraph={valor.paragraph}
                  />
                  <div class="w-full px-4 mt-3">
                    <p class="text-sm text-teal-700 font-medium text-center leading-relaxed">
                      {valor.paragraph}
                    </p>
                    <hr class="border-t border-teal-800 my-3 w-3/4 mx-auto" />
                  </div>
                </div>
              </div>
            ))
          }
        </div>
        <!-- Paginación -->
        <div class="swiper-pagination valores-swiper-pagination"></div>
      </div>
    </div>
  </section>

  <!-- Estilos para el carrusel -->
  <style>
    .valores-carousel-container {
      padding: 0 10px;
    }

    .valoresSwiper {
      width: 100%;
      height: 100%;
      padding-bottom: 40px;
    }

    .swiper-slide {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .valores-swiper-pagination {
      bottom: 0;
    }

    .valores-swiper-pagination .swiper-pagination-bullet {
      background: #0c998a;
      opacity: 0.5;
      width: 10px;
      height: 10px;
    }

    .valores-swiper-pagination .swiper-pagination-bullet-active {
      opacity: 1;
    }
  </style>

  <!-- Script para inicializar el carrusel -->
  <script>
    // Cargar Swiper solo cuando sea necesario (en móvil)
    if (window.innerWidth < 768) {
      // Cargar dinámicamente Swiper
      const swiperScript = document.createElement("script");
      swiperScript.src =
        "https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js";
      swiperScript.onload = function () {
        // Inicializar el carrusel de valores
        const valoresSwiper = new Swiper(".valoresSwiper", {
          loop: false,
          slidesPerView: 1,
          spaceBetween: 20,
          centeredSlides: true,
          pagination: {
            el: ".valores-swiper-pagination",
            clickable: true,
          },
          autoplay: {
            delay: 2000, // 2 segundos
            disableOnInteraction: false,
          },
          breakpoints: {
            640: {
              slidesPerView: 1.2,
            },
          },
        });

        // Lógica simple para reiniciar al llegar al final
        valoresSwiper.on("slideChange", function () {
          if (this.isEnd) {
            setTimeout(() => {
              this.slideTo(0, 500);
            }, 2000);
          }
        });
      };
      document.head.appendChild(swiperScript);

      // Cargar también los estilos de Swiper
      const swiperStyles = document.createElement("link");
      swiperStyles.rel = "stylesheet";
      swiperStyles.href =
        "https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css";
      document.head.appendChild(swiperStyles);
    }
  </script>
</Layout>
