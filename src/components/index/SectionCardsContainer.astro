---
import sectionCards from "@data/sectionCards.data";
import SectionCard from "./SectionCard.astro";
---

<section class="h-auto md:h-[calc(100dvh-6.5rem)] w-full pt-[2%] px-10">
  <h2 class="text-4xl font-extrabold mb-4 text-center text-teal-600">
    OPTIMIZA TU TRABAJO, NEGOCIO Y HOGAR
  </h2>
  <div class="flex flex-col md:flex-row w-full md:h-[90%] gap-4 group">
    {
      sectionCards.map((item) => (
        <SectionCard
          id={item.id}
          title={item.title}
          alt={item.alt}
          image={item.image.src}
          paragraph={item.paragraph}
          imageTitle={item.imageTitle}
        />
      ))
    }
  </div>
</section>
